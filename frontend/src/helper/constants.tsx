export const ROUTES: { BUILDER: string; RUN_WORKFLOW: string } = {
  BUILDER: '/',
  RUN_WORKFLOW: '/run-workflow',
}

export const ROUTE_TITLE: {
  BUILDER: string
  RUN_WORKFLOW: string
} = {
  BUILDER: 'Workflow-Builder',
  RUN_WORKFLOW: 'Run-Workflow',
}
